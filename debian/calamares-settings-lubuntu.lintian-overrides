# The .desktop file uses bash to run firefox as the local
# user. bash is included in the system.
calamares-settings-lubuntu: desktop-command-not-in-package bash [usr/share/applications/lubuntu-calamares.desktop]
